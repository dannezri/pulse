# Dépendances Python pour l'infrastructure de données

# Supabase Client
supabase>=2.0.0

# HTTP Requests (pour Open Wearables API)
requests>=2.31.0

# Utilitaires
python-dotenv>=1.0.0
python-dateutil>=2.8.0  # Parsing robuste des dates ISO (gère microsecondes variables)

# API Server (FastAPI)
fastapi>=0.104.0
uvicorn>=0.24.0

# Job Queue (Celery + Redis)
celery>=5.3.0
redis>=5.0.0

# Testing
pytest>=7.4.0

# AI / LLM
openai>=1.0.0
